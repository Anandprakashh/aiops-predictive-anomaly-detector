apiVersion: v1
kind: Service
metadata:
  name: aiops-detector
  namespace: aiops
spec:
  selector:
    app: aiops-detector
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
